syntax = "proto3";

package org.example.protos;

message Configuration {
  int32 fault_tolerance_level = 1;  // Hata tolerans seviyesi
  enum Method {
    STRT = 0;  // Başlatma komutu
    STOP = 1;  // Durdurma komutu
  }
  Method method = 2;
}
